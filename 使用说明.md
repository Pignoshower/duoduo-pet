# 桌宠「多多」使用说明

> **启动（推荐）**：双击桌面上的 **「多多桌宠」** 快捷方式，或项目目录里的 **`多多.exe`**。
> 也可以双击 `多多.py`（或 `python 多多.py`）。窗口无边框、置顶、半透明，可拖动。
> 聊天：左键双击小猫 / 右键菜单「和我聊天」/ 全局热键 **Ctrl+Alt+D**。

## 0. 先看这里：素材必须单独准备

rames_opt/（452 帧、约 70MB）不进 git 仓库。从 GitHub clone 下来的话，请先到
**Releases 下载 rames_opt.zip** 解压到项目根目录，或用自己的视频跑 python pipeline.py 生成。
**缺素材程序起不来**，启动时会弹说明框告诉你具体怎么做。

## 0.5 关于 exe（打包版）

- `多多.exe` 是**单文件**打包版（约 40MB，无控制台窗口），双击即启动。
- **只打包了代码，没有打包素材**：程序运行时读**同目录**的 `frames_opt/`、`config.json`、
  `pet_data.json`。好处是——以后换素材/修帧、改配置**都不用重新打包**，改完重启就行。
- 所以 exe **要和 `frames_opt/` 放在一起**（现在就在项目目录里，直接双击即可）；想挪到别处，
  把 `frames_opt/` 和 `config.json` 一起带上。
- 重新打包：`python build_exe.py --clean`（图标会用 `frames_opt/idle` 首帧自动生成）。
- 单实例：exe 和源码版共用同一把锁，同时只会有**一只猫**在跑；重复启动会把已有那只叫出来。

---

## 1. 全局热键

| 热键 | 作用 |
|---|---|
| `Ctrl+Alt+D` | 呼出聊天输入框（在猫下方） |
| `Ctrl+Alt+C` | **翻译**剪贴板里刚复制的那段文字 |
| `Ctrl+Alt+Z` | **总结**剪贴板里刚复制的那段内容 |

热键走系统 `RegisterHotKey`，注册失败（被别的软件占用）不影响其它功能；菜单里也能点。

---

## 2. 交互方式

| 操作 | 效果 |
|---|---|
| 左键拖拽 | 拎起小猫（松手后靠边 40px 内自动吸附到屏幕边缘） |
| 鼠标划过/靠近 | 会转头看你 |
| 双击小猫 | 抚摸（好感度 +1，冒爱心） |
| **拖文件到猫身上** | 文本类（txt/md/py/json/csv/log…）自动读内容、总结要点；**之后可以直接追问细节**；文件夹列出内容；图片夸一句；音频直接播放 |
| `忘掉这个文件` | 清掉当前参考文件（追问上下文）；`看我拖的文件` 可以问它现在记着哪个文件 |
| `剪贴板历史` / `最近复制的` | 列出**最近复制的 10 条**（后台每 1.8 秒看一眼剪贴板，自动去重；只在内存，不落盘） |
| `用第2条翻译` / `第3条总结` | 对历史里的**某一条**做翻译/总结/解释/润色/帮回复/念一遍/存桌面（不动当前剪贴板） |
| **点气泡里的文件名** | 直接打开（后面的「📂」= 打开所在文件夹，「记事本」/「其它程序…」= 换程序） |
| 右键 | 菜单按功能分成 4 组：**🐾 互动**（喂食/摸摸/散步/站住/睡觉/叫醒）、**📋 剪贴板**（翻译/总结/解释/润色/帮回复/念一遍/存桌面）、**🔧 工具**（截图/系统状态/音量/**最近找到的文件**）、**⚙️ 设置**（语音播报/看家模式/开机自启/状态面板） |
| 托盘图标 | 显示或隐藏小猫；⚙️ 设置（开机自启 / 看家模式 / 语音播报）；退出 |
| 重复启动 | 不会开出第二只猫，而是把已经在跑的那只**叫出来** |

> 音色相关的入口（试听 / 换个声音 / 情绪演示）**已从菜单移除**——音色已经定好了。
> 哪天想换，聊天里说 `试听一下` / `换个声音` / `情绪演示` 仍然有效。

**气泡（对话框）的行为**：
- **宽度自适应**：短句窄、长内容自动加宽（最多 380px 或屏幕宽-60）。
- **长文不再被裁**：超过屏幕高度的 55% 时，气泡内部可**滚动**，全文都在。
- **鼠标停在气泡上不会消失**，方便读完；移开后恢复自动消失。
- 文字**可以选中复制**，链接可以直接点。
- 气泡位置上下都会夹在屏幕内，不会顶出屏幕看不见。

---

## 3. 聊天能说什么

### 3.1 动作类（本地识别，立刻执行）

`散步` / `走走`、`站住`、`去睡觉`、`醒来`、`打哈欠`、`伸懒腰`、`踩奶`、`扑过来`、`转个圈`、`喂我小鱼干`、
`你好可爱`（好感 +）、`你真傻`（好感 −）。

### 3.2 系统能力类（本地识别）

| 说法 | 作用 |
|---|---|
| `现在几点` / `今天几号` | 报时间、日期 |
| `25分钟后提醒我喝水` / `番茄钟` / `取消提醒` | 定时提醒（**会存盘，重启后不丢**；过期的自动跳过） |
| **`每天18:30叫我下班`** | **每天定点**提醒（到点三重提醒：气泡 + 语音 + 托盘通知） |
| **`每周一9点提醒我开例会`** | **每周**提醒；也支持 `每周一三五 8点 提醒我锻炼`、`工作日9点提醒我打卡` |
| **`明天9点叫我起床` / `今晚8点提醒我看剧`** | 定点一次性提醒（说"今天X点"但已过则 1 分钟后提醒） |
| `开启/关闭全屏避让` | 玩全屏游戏/看视频时**自动躲起来**，退出全屏再出来（主人手动藏的不会被强行拉出） |
| `开启/关闭看家模式` | 看家模式：你离开 5 分钟它去睡、回来打招呼、连续用电脑 50 分钟提醒你休息 |
| `打开/关闭开机自启` | 开机自启（启动文件夹快捷方式，用 pythonw 无窗口运行） |
| `开启语音` / `关闭语音` | TTS 播报开关（默认关闭） |
| `试听一下` / `换个声音` | 试听当前音色 / 换下一个音色（菜单里也有） |
| `情绪演示` | 依次用 8 种语气念一遍（开心/困倦/撒娇…），听效果 |
| `截个屏` | 截屏存到桌面 |
| `念一下剪贴板` / `把剪贴板存下来` | 朗读剪贴板 / 存成桌面 txt |
| `系统状态` / `电量` / `内存` / `网络` | 电量、内存占用、CPU、联网状态 |
| `帮我打开B站` / `打开记事本` | 打开网站或本机程序 |
| `找文件 pet_data` | 在桌面/文档里找文件，列出前 3 条并记住 |
| `打开第1个` / `打开它` | **用默认程序打开**刚找到的那个文件 |
| `用记事本打开第1个` | **指定程序打开**（内置：记事本/写字板/画图/浏览器/VS Code/Word/Excel/PPT/终端/播放器/计算器；也认任意程序名，如 `用notepad++打开第1个`） |
| `换个方式打开第1个` / `选择打开方式` | **弹出 Windows 自带的「打开方式」选择框**，主人自己挑程序 |
| `打开第1个所在文件夹` | 在资源管理器里**打开它的位置并选中** |
| `搜一下 小猫 品种` | 用浏览器搜索 |

> 「打开第 N 个」只认明确指代上一次搜索结果的说法（第2个 / 打开它 / 打开这个文件 / 用XX打开…），
> 不会抢走「帮我打开B站」这类正常指令；没搜过就打开会提示先找文件。
> 指定的程序没装（不在 PATH）时会提示改用「换个方式打开第N个」自己挑。

### 3.3 剪贴板 AI（需要大模型）

先复制一段文字，然后说：

| 说法 | 作用 |
|---|---|
| `翻译这段` / `翻译剪贴板` / `Ctrl+Alt+C` | 译成中文（原文是中文则译英文） |
| `总结一下这段` / `总结剪贴板` / `Ctrl+Alt+Z` | 2~3 句要点 |
| `解释一下这段` | 通俗解释 |
| `润色一下这段` | 改写通顺 |
| `帮我回复这段` | 代写一句回复 |

### 3.4 音量类（本地识别，直接按系统音量键）

`声音大一点`、`太吵了`、`静音`、`别出声`。

### 3.5 其它（交给大模型闲聊/问答）

没命中上面任何一条时，会带上当前时间与好感度去问大模型；大模型可以在回复末尾附带
`[action: xxx]` 让小猫做动作，或 `[tool: open bilibili]`、`[tool: volume down]`、`[tool: clipboard summary]`
等工具标签让小猫真的去干活（工具是白名单，未知工具会被忽略）。

**多步工具链**：工具执行完，多多会把结果**再喂回大模型问一轮**（最多追加 1 轮），
所以它能自己接上第二步——比如"帮我找找 pet_data 然后打开它"，它会走完"找文件 → 看结果 → 决定怎么打开"，
不需要你再补一句"打开第1个"。

---

## 4. 配置（`config.json`）

```json
{
  "api_base": "https://api.deepseek.com/v1",
  "api_key": "在这里填你的 key",
  "model": "deepseek-chat",
  "timeout": 30,
  "temperature": 1.1,
  "max_tokens": 300,
  "history_turns": 6,
  "tts": {
    "engine": "auto",
    "voice": "zh-CN-XiaoshuangNeural",
    "rate": "+10%",
    "pitch": "+8Hz",
    "volume": "+0%",
    "sapi_pitch": 20,
    "sapi_speed": 1,
    "cache": 80
  }
}
```

- `api_key` 为空时，多多自动退回**纯本地模式**（不联网，只认上面 3.1~3.4 的指令）。
- `history_turns` 是记忆轮数（越大越记得住上下文，也越费 token）。
- 好感度、名字、上次位置、语音开关存在 `pet_data.json`。

---

## 5. 语音音色怎么更好听

多多按「最像人 → 最不挑环境」自动挑引擎，顺序是 edge → onecore → sapi → powershell；
菜单里的「🎵 试听 / 换个声音」可以马上听效果，聊天说 `试听一下` / `换个声音` 也一样。

| 引擎 | 音色 | 需要什么 |
|---|---|---|
| **edge（当前启用）** | 神经语音：**晓伊（活泼少女音，默认）**、晓晓（温柔女声）、云希（少年音）、晓北（东北口音）、晓妮（陕西口音） | `edge-tts`（已装好 7.2.8）+ 联网 |
| onecore | 系统"讲述人"那套语音栈：本机有 瑶瑶 / 慧慧 / 康康 | 什么都不用装（离线） |
| sapi | SAPI5 音色（本机只有慧慧）+ 变调 | 兜底 |
| powershell | System.Speech | 最后的兜底 |

`换个声音` 会在 edge 的候选音色之间循环；想固定某一个就改 `tts.voice`。

### 5.1 装 edge-tts 时踩的坑（供以后装包参考）

原来 `pip install edge-tts` 报 `No matching distribution found`，根因不是网络不通，而是
**系统里配了一个本地代理 `127.0.0.1:17891`（VPN 客户端的 IE 代理），pip 会读它**；
代理没在跑时 pip 就卡死。实测 `curl` 直连 pypi、阿里云、清华、腾讯、GitHub 全是 200。
绕过办法（这次就是这么装上的）：

```powershell
$env:HTTP_PROXY=""; $env:HTTPS_PROXY=""; $env:ALL_PROXY=""; $env:NO_PROXY="*"
python -m pip install --index-url https://pypi.org/simple edge-tts
```

### 5.2 代理还影响合成速度（代码已自动处理）

实测同一个 Edge TTS 端点：**直连 ~11 秒，走系统代理 ~1.2 秒**（差 9 倍）。
而 `edge-tts` 用的 aiohttp **不会**读 Windows 的 IE 代理设置，所以多多会自己从注册表
读出代理并传给它（`tts.edge_proxy: auto`）；代理不可用时自动退回直连（慢但能用）。

### 5.3 想更嗲 / 更像人

- **换音色**：说"换个声音"多按几次，或直接改 `tts.voice`（候选见上表）。
- **更嗲**：`tts.pitch` 往正数调（`+10Hz` → `+20Hz`），`tts.rate` 调语速。
- **Windows 自带自然语音**（离线、不用装 python 包）：设置 → 辅助功能 → 讲述人 → **添加自然语音** →
  中文(简体) → 晓晓 / 云希 / 晓辰 / 晓涵（命令直达 `Start-Process ms-settings:easeofaccess-narrator`）。
  装好后多多不用改代码，"换个声音"的候选里就会多出它们（走 onecore 引擎）。
- 说明：`zh-CN-XiaoshuangNeural`（小双）在 Edge 服务端**并不存在**，写了会报
  `NoAudioReceived`；多多遇到无效音色会自动换一个候选，不会哑掉。

### 5.5 情绪语音（会随状态换语气）

同一句话，多多会按**当前状态**换语调；也能自己被大模型安排语气。

| 情绪 | 语气 | 什么时候用 | 语速/音调偏移 |
|---|---|---|---|
| `normal` | 平常 | 发呆、散步 | 基准 |
| `happy` | 开心 | 吃东西、被夸 | 快一点、高 12Hz |
| `excited` | 兴奋 | 蹦跳、扑过来、被逗 | 快 12%、高 18Hz |
| `cozy` | 撒娇 | 踩奶、伸懒腰、被摸摸 | 慢 6%、低 2Hz |
| `sleepy` | 困倦 | 打哈欠、睡觉、刚睡醒 | 慢 12%、低 8Hz |
| `alert` | 提醒 | 定时提醒到点 | 快 8%、高 6Hz |
| `proud` | 得意 | 帮你办成事 | 快 4%、高 8Hz |
| `sorry` | 委屈 | 出错、办砸了 | 慢 8%、低 6Hz |

- **想听全**：右键菜单「🎭 情绪语音演示」，或说 `情绪演示`（会依次念 8 种语气）。
- **大模型也能控制**：它可以在回复末尾加 `[mood: happy]` 之类的标签，多多读到就换语气
  （可以和中括号里的 `[action: …]`、`[tool: …]` 同时用）。
- 实测（同一句"这句是测试语气的喵"）：`happy` 音高 225→250Hz；`sleepy` 时长 2.17→2.41s。
- 各引擎都支持：edge 用 rate/pitch 参数、onecore 用 SSML prosody、sapi 用变调+档位。
- 想整体再慢一点/快一点，改 `tts.rate`（当前 `-4%`，就是你要的"慢一点点"）。

### 5.6 调音参数（`config.json` → `tts`）

| 键 | 默认 | 作用 |
|---|---|---|
| `engine` | `auto` | `auto`/`edge`/`onecore`/`sapi`/`powershell` |
| `voice` | `zh-CN-XiaoyiNeural` | edge 音色 |
| `rate` / `pitch` | `-4%` / `+10Hz` | edge 语速 / 音调（情绪会在此基础上下浮动） |
| `edge_proxy` | `auto` | `auto` 自动用系统代理（快 9 倍）；可填 URL，或留空禁用 |
| `onecore_voice` | `Yaoyao` | onecore 音色（模糊匹配） |
| `onecore_pitch` / `onecore_rate` | `+30%` / `1.2` | onecore 音调（SSML，实测真生效）/ 语速 |
| `sapi_pitch` | `20` | 降级到 sapi 时的变调幅度 |
| `cache` | `80` | 合成缓存条数（同一句话第二次秒出） |

缓存目录：`%TEMP%\duoduo_tts`（wav/mp3，可随时删）。

---

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| 双击没反应 / 出来两只猫 | 单实例已生效：重复启动只会把已在跑的那只叫出来；若确实要重开，先退出托盘里的那只 |
| 出问题想查原因 | 看日志 `%TEMP%\duoduo.log`（滚动 1MB×3），启动、工具调用、异常都会记在里面 |
| 启动时气泡提示配置有问题 | 按提示改 `config.json`（缺 key、网址写错、引擎名不认识等都会被点名） |
| 提醒丢了 | 现在会存进 `pet_data.json`，重启后自动恢复；已过期的会跳过 |
| 问它问题只会"歪头~喵？" | `config.json` 里 `api_key` 没填或填错 |
| 回复"钥匙不对" | key 无效/过期，换一个 |
| 回复"想太久超时了" | 网络慢，或在 `config.json` 里调大 `timeout` |
| 说"翻译这段"没反应 | 剪贴板是空的（先复制文字），或没配 key |
| 快捷键没反应 | 被其它软件占用了，用右键菜单代替 |
| 没有声音 | 语音默认关闭，说"开启语音"，或右键菜单里打开 |
| 声音不够可爱 | 装系统"自然语音"（第 5 节第 1 条，不用装包），或调 `tts.onecore_pitch`；也可装 `edge-tts` |
| 说"打开第1个"没反应 | 得先 `找文件 XXX`，多多才知道"第 1 个"是哪个 |
| 说"用XX打开"提示找不到程序 | 那个程序不在系统 PATH 里；改用「换个方式打开第N个」，从 Windows 的选择框里挑 |
| 语音比文字慢半拍才出声 | 首次合成 1~2 秒（走代理），同一句话第二次是秒出的；若慢到十几秒说明代理没生效（见 5.2） |
| 装包时 pip 报代理错误 | 见 5.1：清空 `HTTP_PROXY/HTTPS_PROXY` 并用 `--index-url https://pypi.org/simple` |

---

## 7. 开发者信息

| 文件 | 作用 |
|---|---|
| `多多.py` | 主程序：窗口、绘制、动画状态机、行为、本地意图、工具派发 |
| `ai_assistant.py` | 大模型客户端（OpenAI 兼容）、提示词、标签解析、找文件/开站点/序号解析 |
| `pet_tools.py` | TTS 播报（edge / **onecore(WinRT)** / sapi+变调 / powershell）、情绪语调、**日程解析**（每天/每周/工作日/几点）、系统状态、提醒解析、音量 |
| `app_health.py` | 运行保障：滚动日志、单实例锁、开机自启、配置自检、空闲检测、**全屏检测**、看家判定 |
| `video_to_transparent_frames.py` → `integrate_videos.py` → `prepare_frames.py` | 视频素材 → 透明帧 → 对齐烘焙 → `frames_opt/` |
| `fill_gaps.py` | 抠图补洞工具：`--mode auto`（默认，只补被误抠的猫毛）/ `all`（把透空的背景闭环也填实）/ `none`（只报告） |
| `make_turn.py` | **程序生成"原地转圈"动画**（用 idle 画面，替代画风不一致的素材9）；`python make_turn.py [--frames 24]` 可重做 |
| `compare_clips.py` | 各素材"猫长得一不一样"的客观对比（配色/面具/眼睛/比例）+ 对照图，用来发现画风漂移 |
| `audit_frames.py` | 帧体检：帧数/间隔/时长、重复帧、以及**猫身上不该出现的白块** |
| `fix_white_patches.py` | 修"被误补成不透明的背景白块"（腿间/体尾之间的背景），`--apply` 才写入 |
| `round_eye_corners.py` | 收圆眼角上"尖尖"的细刺（3x3 开运算 + 最近颜色填平，只削 ≤2px 的尖） |
| `check_face_artifacts.py` | 找脸上的多余笔画（眉毛/细线）。注意：目前会把**穿过眼上方的胡须**误报，结果需人工确认 |
| `visual_regression.py` | **视觉回归基线**：把各状态的白块占比、毛色、面具、眼睛、对齐抖动存成基线并比对，防止"白块/花脸"复发；`--update` 接受新基线 |
| `snapshot.py` | **素材快照/回退**（素材的唯一保险）：`save "备注"` / `list` / `restore latest` / `prune --keep 8`，快照存在项目内 `快照/`（回退前会自动再备份当前状态） |
| `pipeline.py` | **一键流水线**：抠图 → 修白块 → 清白雾白点 → 眼角收圆 → 烘焙 → 重做转圈 → 视觉回归（顺序不能错，脚本替你记住）；`--skip-extract` 只重跑后半段，跑前自动存快照 |
| （`integrate_videos.py` 内置） | 抽完帧会**自动画风体检**：点名颜色/面具偏差过大的素材，并给出各状态"有没有虹膜"的参考指标 |
| `视频生成提示词.md` | 生成新动作素材用的提示词包（角色设定严格按 `参考图/角色参考.png`） |
| `参考图/角色参考.png` | **请把角色参考图放这里**，生成视频时上传它 |

**关于"透明空缺"**（已查清，附证据）：472 帧里剩下 14 处内部透空，逐像素比对视频原像素后确认——
它们不是猫身上被挖的洞，而是**白色背景被身体/尾巴/前爪围成了闭环**（例如下巴与尾巴之间、两条前腿之间），
视频里看着没问题是因为"白底 + 奶白猫毛"几乎同色，抠图放到彩色桌面上才显出来。
真正的抠图误判（`walk/frame_0036` 的 42px 猫毛、`turn/frame_0033` 的 12px）已用 `fill_gaps.py --mode auto` 按视频原色补回。
若你更想要"完全不透空"的实心效果，跑 `python fill_gaps.py --mode all && python prepare_frames.py` 即可（可反悔：重跑一次 `integrate_videos.py` 就回到原始抠图结果）。

**关于画风一致性**（素材9 / 素材11 的问题）：

用 `python compare_clips.py` 把每个素材的猫做量化对比后发现，**配色其实完全一致**
（奶白毛都是约 `(242,229,208)`，深棕部位 57~79），差异在**造型**：

| 素材 | 问题 |
|---|---|
| 素材9（`生成提示词里的第九个.mp4`，原 turn） | 眼睛是**闭着的粗弧线**（`^ ^`）、面具覆盖更大且偏灰、耳内多一道线、几乎没有胡须 |
| 素材11（`生成提示词里的第十一个.mp4`，jump） | 眼睛**明显偏大**（约 1.6~2 倍），且起跳时**头顶出框** |

处理办法：

- **turn（转身）**：素材9 已退出流水线（`integrate_videos.py` 的 SOURCES 里不再有它），
  改用 `make_turn.py` **用 idle 的画面程序生成** 18 帧转圈动画：首尾帧与 idle **像素级一致**（预乘色差 0），
  进出无缝、零外来画风。重跑 `python prepare_frames.py` 时会**保留**它（日志会打印 `[保留] turn`），不会被旧素材覆盖。
- **jump（跳）**：素材11 本来就没进程序（起跳出框），逗猫时的跳跃由程序实现（`_do_hop` 的物理弹跳 + 挤压拉伸），
  观感与其它动作一致，**不需要再取材**。
- 想要"真·转身/跳跃"视频的话，按 `视频生成提示词.md` 重新生成（务必带上 `参考图/角色参考.png`）。

**各动作帧数**（`frames_opt/meta.json`，单帧 42ms，合计 **452 帧**，无重复帧）：

| 动作 | 帧数 | 时长 | 备注 |
|---|---|---|---|
| idle 发呆 | 56 | 2.35s | 循环 |
| walk 走路 | 48 | 2.02s | 循环 |
| eat 吃 | 44 | 1.85s | 播完回发呆 |
| sleep 睡 | 40 | 1.68s | 循环 |
| wake 起身 | 48 | 2.02s | 一次性 |
| yawn 打哈欠 | 56 | 2.35s | 一次性 |
| stretch 伸懒腰 | 52 | 2.18s | 一次性 |
| pounce 扑 | 44 | 1.85s | 一次性 |
| knead 踩奶 | 40 | 1.68s | 循环 |
| turn 转圈 | 24 | 1.01s | 程序生成（`make_turn.py`），首尾与发呆像素级一致 |

体检命令：`python audit_frames.py`（帧数/间隔/时长/重复帧/白块一次看完）。

**拿到新视频素材后的标准流程**（一条命令，顺序由脚本保证）：

```powershell
python pipeline.py                 # 抠图 → 修白块 → 清白雾白点 → 眼角收圆 → 烘焙 → 重做转圈 → 视觉回归
python snapshot.py save "备注"      # 满意了存一份快照（改坏了：python snapshot.py restore latest）
```

`pipeline.py` 会在跑之前**自动存一份快照**，任何一步失败就停下并告诉你从哪继续
（`python pipeline.py --skip-extract`）——素材和帧都保护住了。

**关于"眼角尖尖"**（已处理）：`round_eye_corners.py` 把眼角上细于 2px 的尖刺收圆
（用 3×3 开运算找尖刺，再用**最近的非描边颜色**填平，所以是顺着原色收圆、不会留补丁），
本次共处理 265 帧、83524 像素，描边主干不受影响。
改前的原始帧备份在 `%TEMP%\duoduo_frames_backup\`，随时可回退（或重跑 `integrate_videos.py`）。

**关于"走路有一帧出现一块白色"**（已修）：根因是抠图阶段的补洞逻辑——两腿之间/身体与尾巴之间的背景
被腿+肚皮围成闭环，包围度很高，于是被当成"该补"的地方，用视频里的背景像素填成了**不透明的白**。
修法：`fix_white_patches.py` 找出"猫身下半部的中性纯白块"（≥400px，自动排除眼睛高光与牙齿）并恢复成透空；
`integrate_videos.py` 的 `fill_hull` 也加了同样的规则（`skip_bg_lower=True`），以后重跑流水线不会再犯。
本次共修 **129 处**（walk 那帧的 2260px 白块亮块占比从 4.16% 回到 1.23%，与相邻帧一致）。
注意：原始帧里这类白块常通过一条极窄的半透明通道与背景相连，所以判据不能要求"必须闭环"。

测试（全部离屏运行，不需要联网）：

```powershell
$env:QT_QPA_PLATFORM="offscreen"
python selftest.py         # 29 项：帧资源、动画状态机、转身首尾一致性
python test_app_smoke.py   # 31 项：应用级冒烟（交互/系统能力/存档）
python test_assistant.py   # 89 项：大模型层 + 集成（打桩，不联网）+ 情绪/菜单/热键
python test_tools.py       # 73 项：TTS（音色/情绪/代理）、系统状态、提醒、音量
```

## 9. 文件整理、敏感操作与安静模式

### 9.1 删除与清理（一律进回收站，可还原）

| 说法 | 结果 |
|---|---|
| `删掉第2个` / `删除第1个和第3个` | 先 `找文件 XXX`，再按序号删；列出清单等确认 |
| `删除 D:\test\a.txt` | 按完整路径删；带空格的路径不加引号也认 |
| `把找到的都删了` | 最近找到的全部列清单（仍需确认） |
| `确认` / `取消` | 执行（进回收站）/ 放弃 |
| `清理一下你的截图` | 清桌面 `多多截图_*.png` |
| `清理剪贴板文本` | 清桌面 `剪贴板_*.txt` |
| `清空语音缓存` | 清 `%TEMP%\duoduo_tts` |
| `把第1个移动到桌面` | 移到桌面 / 文档 / 下载 / 图片 / 临时目录 |

约束：只删文件不删文件夹；系统目录与程序目录拒删；单次上限 20 个；删除记录写在 `%TEMP%\duoduo.log`。

### 9.2 打开控制台与运行命令（确认后执行）

| 说法 | 结果 |
|---|---|
| `打开控制台` / `打开cmd` / `打开终端` / `打开命令提示符` / `打开小黑框` | 确认后开出 cmd 窗口 |
| `打开 PowerShell` / `开个命令行` | 确认后开出 PowerShell |
| `以管理员打开命令行` / `用管理员身份打开控制台` | 管理员 PowerShell，还会再走一次 UAC |
| `运行 ipconfig /all` | 确认后执行，输出贴进气泡；超 20 秒自动停 |
| `确认` / `取消` | 执行 / 放弃 |

破坏性命令（`format`、`diskpart`、`shutdown`、`del /f /s`、`rd /s`、`rm -rf`、`reg delete`、`net user`、`bcdedit` 等）**当场拒绝，连确认机会都不给**。大模型不能触发删除、控制台与运行命令，这些只能由你明确说出口。

### 9.3 安静模式 / 免打扰时段

| 说法 | 结果 |
|---|---|
| `开启安静模式` / `别吵我` / `安静点` | 不发声、不主动搭话（久坐提醒等暂停），你问它照常冒泡 |
| `晚上11点到早上7点别打扰我` | 每天固定时段免打扰，支持跨零点 |
| `关闭安静模式` / `可以说话了` | 恢复发声 |
| `现在安静吗` | 回报状态与时段 |

右键 ⚙️ 设置 → 🔇 安静模式 也能一键切换；设定存进 `pet_data.json`，重启仍然有效。
